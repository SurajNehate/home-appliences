# Copy this file to `.env` for local development. DO NOT COMMIT `.env`.
# Netlify Dev will load variables from `.env` when you run `npm run dev:netlify`.
# Fill in your Neon credentials below.

# Option 1: Use Neon REST (PostgREST) endpoint (preferred for the existing functions)
# Example: https://<your-rest-endpoint>/rest/v1
NEON_REST_URL=https://ep-soft-waterfall-aelrt553.apirest.c-2.us-east-2.aws.neon.tech/neondb/rest/v1

# Choose ONE authentication method for Neon REST:
# Preferred: Bearer API key issued for the REST endpoint
NEON_API_KEY=
# OR: Basic auth in base64(username:password) — only if your setup uses it
NEON_BASIC_AUTH=

# Option 2: Direct Postgres connection string (used by some test functions)
# Example: postgres://user:pass@ep-xxxxx-pooler.<region>.aws.neon.tech:5432/db?sslmode=require
NETLIFY_DATABASE_URL=postgresql://neondb_owner:npg_2JCQnoWX7uOd@ep-soft-waterfall-aelrt553-pooler.c-2.us-east-2.aws.neon.tech/neondb?channel_binding=require&sslmode=require

# JWT secret for issuing/verifying tokens in auth-related functions
# Use a strong random string in dev (it can differ from production)
JWT_SECRET=2a67a978dc1feef70067a501eb492956

# Optional: token expiration (defaults to 7d if unset)
# JWT_EXPIRES_IN=7d
